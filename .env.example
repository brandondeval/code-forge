# Use a long random value outside local development (for example: openssl rand -hex 32)
JWT_SECRET=replace-with-a-long-random-secret
# Local Docker Git remote credentials. Change this outside local development.
GIT_PASSWORD=forge
GIT_CLONE_BASE_URL=ssh://forge@localhost:2222
